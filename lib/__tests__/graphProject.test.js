const assert = require('assert');
const graphProject = require('../index.js');

describe('graphProject', () => {
  it('has a test', () => {
    assert(false, 'graphProject should have a test');
  });
});
